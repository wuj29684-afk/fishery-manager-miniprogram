# fishery-manager-miniprogram

微信小程序源码仓库，来自 fishery-manager 项目的 `miniprogram/` 目录。

当前开发状态见 [STATUS.md](STATUS.md)。

## 开发

```bash
npm install
npm run check:domain
npx tsc --noEmit
NODE_ENV=production npm run build:weapp
```
